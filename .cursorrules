# Ergenekon Engine Guidelines

## Coding Standards
- Strictly use TypeScript across all `@ergenekon` monorepo packages.
- Ensure strict Null checks and type-safety.
- All Pull Requests must pass the `npm run test` pipeline.
- Maintain the existing Prettier configuration.
