{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ergenekon.serviceAccountName" . }}
  labels:
    {{- include "ergenekon.labels" . | nindent 4 }}
{{- end }}
